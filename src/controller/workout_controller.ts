import mongoose from "../driver/mongodb";
import Workout, { IWorkout } from "../model/workout";

// return a list of workouts based on the time allocated and the exercise category
async function generateWorkout(timeAllocated: number, exerciseCategory: string): Promise<IWorkout[]> {

    // Implement Logic

    return [];

}